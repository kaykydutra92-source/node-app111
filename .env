# ===== Telegram =====
# ===== Telegram =====
BOT_TOKEN=8253854412:AAGm8t9Ix1oNJV6nmEJdSRXls0OkNBMASyE
CHAT_ID=-1002997258131

# ===== Helius / RPC =====
HELIUS_API_KEY=50561f0b-ec1f-42cb-9edb-465bd12cdd91
# If you prefer to set explicitly, keep RPC_URL; otherwise it auto-builds from HELIUS_API_KEY
RPC_URL=https://mainnet.helius-rpc.com/?api-key=50561f0b-ec1f-42cb-9edb-465bd12cdd91

# ===== Behavior =====
DELAY_SECONDS=0
PORT=3000

# Filters / time
USER_TZ=America/Sao_Paulo
ONLY_TODAY=true     # only notify tokens whose tx blockTime is "today" in USER_TZ
MAX_AGE_SECONDS=0 # or set e.g. 86400; 0 disables age filter

# Subscriptions
WS_COMMITMENT=processed        # used by logsSubscribe/programSubscribe
RPC_COMMITMENT=processed       # used by getTransaction/getAccountInfo (defaults to WS_COMMITMENT if unset)

USE_PROGRAM_SUBSCRIBE=false
USE_LOGS_MENTIONS=false
USE_LOGS_ALL=true

# Fallback inspector
FALLBACK_SIGNATURE_LIMIT=4
FALLBACK_CONCURRENCY=6
FALLBACK_INSPECT_TTL_MS=10000
# rotate if your main RPC returns 429/5xx; comma-separated
RPC_ALT_URLS=

# client-side smoothing (these have sensible defaults in code, but explicit is better)
RATE_LIMIT_RPS=6
RATE_LIMIT_BURST=5
RATE_LIMIT_REFILL_MS=250
RATE_LIMIT_MAX_BACKOFF_MS=15000

# trip a circuit breaker on repeated 429s
CIRCUIT_429_THRESHOLD=6
CIRCUIT_WINDOW_MS=10000
CIRCUIT_OPEN_MS=20000

# Verify behavior
VERIFY_MINT_BEFORE_NOTIFY=false
VERIFY_CONCURRENCY=2
VERIFY_RETRIES=2
VERIFY_BASE_DELAY_MS=150

# Owner whitelist (optional): comma-separated strings to require in logs
# If empty, we use PROGRAM_IDS + Tokenkeg as effective whitelist.
OWNER_WHITELIST=
# Add comma-separated regex fragments (no slashes) to boost candidate scoring
EXTRA_MARKERS=initialize2,openbook,openbookv2,create_pool,open_position,whirlpool,creatpool,create,addliqui,addliquidity,initializepool

# Debug logs
DEBUG=false
